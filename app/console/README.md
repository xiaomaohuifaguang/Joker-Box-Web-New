# app/console — 后台

`layout.tsx` = `<RequireAdmin>` + `SidebarProvider`(Sidebar + SidebarInset)；顶栏 = SidebarTrigger + `ConsoleBreadcrumb`（菜单树+路由算路径链，纯文本）+ 主题预设/明暗切换。有 `loading.tsx`/`error.tsx`。`page.tsx` 是仪表盘（占位）。Sidebar 菜单 backend-driven（`useMenuTree` menuType=-1），折叠态父项点开向右浮层，footer 用户菜单。`_components/`：ConsoleSidebar、ConsoleBreadcrumb。

路由权限：`<RequireAdmin>` 用后台菜单树判断（URL 直进无权限路由 → 404），见 `components/README.md`。

通用约定：分页列表 hook 收 `{search,current,size,refreshKey}`，增删改后 `setRefreshKey(k=>k+1)` 重拉；分页页码 + 省略号；删除走 AlertDialog 二次确认。

## authority/（权限）
- `org-manager/`：机构管理。左机构树（后端虚拟根 id=-1「全部」为第一层单节点）+ 右列表（表格+分页+搜索）。CRUD：新增/编辑（`OrgFormDialog` 父级 Select）、删除（AlertDialog）。`_components/`(OrgListPanel, OrgFormDialog)。
- `user-manager/`：用户管理。左机构树（复用共享 `OrgTreePanel`，选中按 orgId 过滤，虚拟根=全部用户）+ 右列表（面包屑+搜索+角色 Select+重置 / 表格 / 分页）。行操作：编辑（`UserEditDialog` **即时绑定角色与机构**：Badge × 移除 + 下拉添加，无保存按钮）、重置密码、删除。列表面板 `key=selectedId` 切机构重挂载；角色选择器页面级拉取一次。`(UserListPanel, UserEditDialog)`。
- `role-manager/`：角色管理。扁平列表+分页+搜索。行：角色名 / 后台管理（`admin=1` 显 Badge）/ 更新时间 / 操作。新增仅 name（可选**复制权限自** `withRole` 继承源角色 apiPath+菜单权限）；编辑走 save（role{name,admin} + apiPathTree + menuChoose 前后台合并）。编辑弹窗**三 tab 权限编辑器**：apiPath 权限（复用共享 `ApiPathBindingTree`，`roleBind` 预勾选、`whiteList` 禁用）/ 前台菜单 / 后台菜单（`MenuCheckboxTree` tri-state）。删除：软删（有绑定失败 toast 提示改用强删）+ 强制删（级联，destructive）。共享件 `ApiPathBindingTree`/`TriCheckbox`（`components/`）+ `buildApiPathSaveTree`（`lib/apiPathTree.ts`）。
- `_components/OrgTreePanel`：机构树，org-manager / user-manager 共用。

## 其它
- `api-manager/`：API 管理。筛选（搜索+角色 Select+服务/分组级联 `ServerGroupCascader`）+ 表格（名称/路径/服务/分组/白名单 Badge/创建时间/编辑）+ 分页。白名单仅可经编辑弹窗（`ApiPathEditDialog` Switch）修改。`(ServerGroupCascader, ApiPathEditDialog)`。
- `menu-manager/`：菜单管理。前台/后台分段（menuType -2/-1 `ToggleGroup`）+ **树形表格（不分页：菜单量级小、层级是核心结构）**。**签名**：菜单列渲染为真实导航项（`[图标 chip] 名称`，按 `icon` 字段，`MenuIcon` switch 渲染——static-components 见 CLAUDE.md「通用坑」；空则不渲染、无兜底）。**拖拽排序/改挂**（@dnd-kit：拖把手 + `DragOverlay` + 落点指示线；落定后 active 成为 over 的兄弟 = `newParentId=over.parentId`，防环校验，重算受影响兄弟 sort，乐观更新+失败回滚，逐个 update）。新增走 add（仅字段，菜单未建不能绑 api）；编辑走 save（字段 + api 绑定一次性存）。编辑弹窗：图标选择器（`IconPicker`：Dialog 内**内联下拉面板**非 Popover——滚轮被挡见「通用坑」；14 类 ~149 图标 + 搜索 + 清除）+ api 绑定树（`ApiPathBindingTree` 三级 checkbox，`roleBind` 预勾选、`whiteList=1` 禁用）。`menu.icon` 同时供前台 Header / 后台 Sidebar 导航渲染。`(MenuTreeTable, MenuFormDialog, IconPicker)`；`useMenuTreeAll` 按 menuType 拉树+refresh。
- `website-manager/`：网址收藏管理。扁平列表+分页+筛选（search + groupName Select 带计数，分组复用前台 `/website/group` 派生）。**签名**：地址列渲染为可点击外链（mono + 外链图标，无协议补 https://）。分组无实体（`groupName` 是网站字段，自由文本默认"默认"，无分组 CRUD）。CRUD：groupName/url(等宽)/title/description(Textarea)。**编辑直接用行数据调 save，不拉 info**（行数据已全且 info 缺 title）。`(WebsiteFormDialog)`；`useWebsitePage`。
- `mail-manager/`：邮件记录（只读日志，无 CRUD）。列表+分页+搜索。**签名**：详情弹窗把 `content`(HTML) 放**隔离 iframe**（`srcDoc` + `sandbox=""`，不跑脚本、不污染页面）渲染，`variable`(JSON) 美化成 `<pre>`。列表只返回摘要（无 content/variable），详情才含 content+variable。`(MailDetailDialog)`；`useMailPage`。
- `system/code-table/`：码表管理。两视图（`?tableId` 切换，`useSearchParams` + Suspense）：**列表视图**（无 tableId）分页+筛选+CRUD+详情（跳项视图）；**项视图**（`?tableId`）头部（detail 拉码表信息 + 编辑码表 + 返回）+ 码表项表。项表按码表 `tree` 标志**自适应扁平表/树形表**（`CodeItemTreeTable`），@dnd-kit 拖拽排序/改挂（同 menu-manager：active 成为 over 兄弟 = `newParentId=over.parentId`，防环，重算受影响兄弟 sort，乐观更新+回滚，逐个 update）。项 CRUD：label/value(等宽)/parentId（仅树形，排除自身子孙防环）/sort/status/remark。`buildCodeItemTree`（`lib/codeTableTree.ts`）扁平组树。`(CodeTableListPanel, CodeItemsView, CodeItemTreeTable, CodeTableFormDialog, CodeItemFormDialog)`；`useCodeTablePage`/`useCodeItems`。
- `ai/model-manager/`：AI 模型管理。类型分段（全部/对话/向量，`ToggleGroup`，「全部」用哨兵 `__all` 因 Radix 空串歧义）+扁平列表+分页+搜索（`useAiModelPage`，分页传 `type`，空=全部传 undefined）。列：名称/模型/类型 Badge/描述/操作。CRUD：新增/编辑（`AiModelFormDialog`——编辑开弹窗即 loading、`/ai/model/info` 回填全量；name/model/type 必填，completionsPath/embeddingsPath/apiKey/description 可空，**dimension 向量维度仅 EMBEDDING 显示且必填**（正整数），CHAT 不传；**编辑 apiKey 脏检查**——与详情原值比对，改了才传、未改不带该字段）、删除（AlertDialog 确认，`/ai/model/remove` body{id}）。**默认模型**：`useAiModelPage` 同帧并行拉 `queryPage` + `/ai/model/defaultModelSettings`（无参→`AiModelDefaultMap` key=CHAT/EMBEDDING，value 可空=未设）；**唯一权威**——每行是否默认由 `defaults[row.type]?.id===row.id` 现算（不存行内冗余），名称列默认行显「实心星+{对话|向量}默认」chip（`text-brand` + `color-mix(--brand)` 淡色边/底，随 5 套预设）、操作列星标状态感知（默认行=实心星 `text-brand` 点击解绑 / 非默认行=描边星点击设默认），均 AlertDialog（文案带类型）→ `setDefaultModel`/`clearDefaultModel`（均 **query 传参**）→ `setRefreshKey` 列表+默认同帧重拉；允许无默认。类型枚举 `AiModelType`+`AI_MODEL_TYPE_LABELS`（types/ai-model）。无树。`(AiModelFormDialog)`。
- `form/dynamicForm-manager/`：动态表单设计器（见同级 README）。
- `form/dynamicForm-instance-manager/`：表单实例管理。两视图（`?instanceId` 切换，`useSearchParams`+Suspense）：**列表**（分页+搜索，`queryDynamicFormInstancePage`；操作列「查看」跳详情）+ **详情**（`getDynamicFormInstanceInfo`，只读预览）。列：表单名称/版本/实例ID/创建/修改时间。详情复用 `DynamicFormRenderer`（新加可选 `disabled` 整表只读）——实例值经字段 `value` 回填+`defaultValue` 兜底，`linkageRules=[]`（接口不返回）。`_components/`(InstanceListPanel, InstanceDetailView)；`useDynamicFormInstancePage`。
- `process-manager/`：流程管理。子路由 `designer/`（流程设计）；`process-manager/` 自身无 `page.tsx`（无 `/console/process-manager` 列表路由）。`designer/` = 列表/设计器/只读查看三视图（`?design=` / `?view=` pushState 切换，同 dynamicForm-manager）：**列表**（分页+搜索+**分类 tabs**（全部+`PROCESS_TYPES` 注册表遍历，全部=不传 `processCategory`，切换回第 1 页；分类列 raw type 映射 name 显示、注册表外旧值原样），`queryProcessDefinitionPage` body 新增 `processCategory?`；操作列按 status 走——草稿=查看/编辑/删除/发布、已发布=查看/停用、已停用=查看/编辑/发布；删除 `removeProcessDefinition`、停用 `stopProcessDefinition`（均 body{id}）、发布 `deployProcessDefinition`（**query 传参 id**，非 body），成功 toast + `setRefreshKey` 重拉）+ **设计器**（`ProcessDesigner`：三栏**纯 flex 固定宽度**（左节点面板 `w-64` / 中画布弹性 / 右面板 `w-80`，加宽容纳长节点名）——左节点面板（**分组+滚动+搜索**，为后续多节点扩展；含**申请节点预设** `APPLY_NODE`（nodes.tsx，面板/拖拽标识 `__applyNode` 特判 onDrop）：挂「任务」组末尾，拖入即建**固定 id=`applyNode`（全图唯一）** 的 userTask，预置 名称申请人+approvalType=0申请人自审+actionButtons=pass（自动通过默认不勾选；**审批类型锁定申请人自审只读不可改**——UserTaskConfig 按固定 id=applyNode 判定禁用下拉）；**开始节点↔申请节点「表单配置」双向同步**（updateSelected 特判：仅 inheritMainForm/fieldPermissions 两个 key，改任一节点镜像到另一节点——开始节点按 type=startEvent 找 id、申请节点固定 id=applyNode，存在才同步；其它字段与普通用户任务不同步）——非独立 BPMN type，落库 type 仍 userTask，画布渲染/UserTaskConfig 天然复用；入边不做限制，支持驳回/流转回申请人）；自定义节点（type=BPMN 元素名）：事件(startEvent/endEvent)/任务(serviceTask/userTask)/网关(exclusiveGateway/parallelGateway/inclusiveGateway)，`nodes.tsx` 的 `PROCESS_NODE_REGISTRY`（含 group/shape/锚点/出入线约束/唯一性/配色）+ 三族形状组件（贴合 BPMN：`EventNode` 事件=圆形（结束双圈）/`TaskNode` 任务=圆角矩形（内嵌文字）/`GatewayNode` 网关=菱形（内嵌 X/+/○，rotate-45 反转），名称标签 `NodeLabel` 统一放图形正下方；**全部固定尺寸不可调**（事件 h-10/11、任务 h-8 w-32、网关 h-11，均 CSS 内定不带 style）；右键经模块级 `processNodeContextHandler` 上报给外层）+`processNodeTypes`（按 shape 映射），HTML5 DnD 拖拽入画布，`screenToFlowPosition` 换算落点，开始/结束唯一、服务任务可多）| React Flow 画布（`@xyflow/react`，`nodeTypes` 自定义节点、连线/缩放/平移/MiniMap；**连接约束**：`checkLink` 按 REGISTRY 的 `maxOut`/`maxIn` 校验出入线数（startEvent 与 serviceTask/userTask `maxOut=1`——任务=顺序活动单去向，分支走排他/包容网关、并行走向并行网关；禁自连、禁重复连线），`onConnect` toast 拒绝 + `isValidConnection` 拖拽期即时提示，加网关等只改 REGISTRY；**拖节点插入连线**（官方 drop-on-edge 思路：`defaultEdgeOptions.interactionWidth=24` 加宽 edge 热区，`hitEdgeIdAt` 用 `elementsFromPoint` 命中 `.react-flow__edge-interaction`；画布内 `onNodeDrag` 高亮命中边/`onNodeDragStop` 插入 + 面板 `onDrop` 落边也插入；`insertIntoEdge` 拆 A→B 为 A→新（`updateEdge` 保留原边 style/label/未来条件）+新→B（`addEdges` 新空边），仅「有进有出」节点（任务/网关，start/end 自动排除）可插、两条新连线过 `checkLink`）；**节点 id 只读展示**（节点配置「节点 ID」输入框 readOnly+disabled，等宽字体，后端 BPMN 元素 id；名称 Input 失焦空回退类型默认名）；新拖入时 `nextKindLabel(kind, nodes)` 把默认名「类型标签+（当前同类型节点数+1)」（用户任务2）写进 node.data.label——画布形状与节点配置名称 Input 同读 label，天然一致；简单计数即可（重名无害，节点靠内部 id 区分），开始/结束唯一节点不加序号；**删节点=节点右键弹固定定位 ContextMenu 选「删除」**（右键经 handler 上报节点 id+光标坐标）**或 Delete/Backspace**（`deleteKeyCode`），连带删相关连线）弹性 | 右面板（**三态上下文**：点空白（`onPaneClick`→`paneActive`）显**流程配置** `ProcessConfig`（名称/分类/描述 + 表单绑定——**分类=下拉选择**（选项=`lib/process-types.ts` 的 `PROCESS_TYPES` 注册表，与前台两中心标题同源；新建默认选中「默认分类」(`default`)，注册表外旧值追加一项原样展示、旧空值按 `default` 回填；**仅新建可改，编辑页只读**（`id!=null` 禁用））；表单绑定 globalFormBinding：选已发布表单 + 版本两级联动，**表单可清空**——「不绑定表单」占位项 `__none__`（Radix SelectItem 禁空串 value）映射回 `""`；**清空/换表单/换版本都经 `resetNodesInheritMainForm` 重置各节点 inheritMainForm**），自顶栏迁入；点节点 `onNodeClick` 复位 `paneActive`）/ 点节点显**节点配置** `NodeConfig`（名称/备注 + 按 kind 挂专属：userTask → `UserTaskConfig`——approvalType 下拉（申请人自审0/会签1/或签2/随机1人3/认领4/随机多人会签5/随机多人或签6/上一节点选择1人7/上一节点选择多人会签8/上一节点选择多人或签9）、passRate 仅会签类（1/5/8）显（0~1 两位小数、默认 1.00、失焦钳制；选会签类补默认、切走清空）、**randomCount 随机人数仅随机多人会签5/或签6 显（正整数、默认 1、失焦钳制；选 5/6 时补默认 1、切走清空；「上一节点选择」7/8/9 由上一节点定人不配）**、候选用户/角色/部门多选（**申请人自审0 时隐藏并清空候选人+通过率+随机人数——本人审批无需联动配置**；`CandidateSelects`：users=远程搜索下拉 `/user/selectorUserWithInfo`(默认10条配search)+回显 `/user/selectorInitByIds`、roles=静态 `/role/selector`、depts=树形父子独立 `/org/getOrgTree`；**候选值=逗号串 id 入 node.data，展示名走 `__names` 运行时映射、保存 stripNode 剥 `__` 前缀**；内联面板不 portal）、autoApproveIfSelf 自审批自动通过 Switch（String "1"开/"0"关显式落库，默认关；处理人为申请人时自动通过）、actionButtons 操作按钮多选（固定枚举 pass通过/back驳回/reject拒绝，chip 复选钮，逗号串入 node.data.actionButtons）、勾选 back 驳回后显驳回配置（backType 驳回方式单选 prev上一节点/specific驳回到指定节点/choose用户自选（**勾选「驳回」时把默认值显式落库**——backType=prev、backAssigneePolicy=auto 写入 node.data，不再靠 ?? 显示兜底，保证保存传给接口；取消勾选清对应字段）；backNodeId 驳回节点仅 specific 显，候选=**上游任务节点**（沿入边反向 BFS 收集当前节点祖先，再过滤 `shape==="task"`——驳回=回退到上游，不允许顺流跳下游防永动环；开始/结束/网关不可驳；候选空时提示「无可驳回的上游任务节点」；NodeConfig 传 nodes+edges 枚举/算可达）；backAssigneePolicy 回退后任务分配策略单选默认 auto智能默认/last_handler派上次办理人/reassign按candidate重分配；取消勾选 back 或切非 specific 时清对应字段）；inheritMainForm 是否继承主表单字段（**boolean Switch**，开始/用户任务共用 `InheritMainFormField`，主表单=流程表单绑定 globalFormBinding 绑定的表单；**仅流程选好了表单+版本（`mainFormBound`=formId/formVersion 均非空）才可勾选，否则禁用并提示**，清空/换表单/换版本时重置所有节点勾选；勾选后下方展开**字段权限** `NodeFieldPermissions`——面板里只放一个触发按钮（`ListChecks` + 显示已设非默认项数徽标），点击开**宽 Dialog**（`sm:max-w-2xl` + 内容区限高滚动，字段多不受右栏宽度限制）配置；打开时 `/dynamicForm/info` 传主表单 formId+version 拉设计配置（重置在 onClick、effect 只异步拉取，避开 set-state-in-effect），按「未分组/各分组」分区排版、每字段一个权限 Select（可见VISIBLE默认/只读READONLY/隐藏HIDDEN/必填REQUIRED），顶部「全部字段」全局批量 + **每个分区头部各自的分区批量**（均复用 applyPermissions，选可见=该范围全恢复可见）；**仅收录非默认权限**（VISIBLE 从 node.data.fieldPermissions 移除），改动实时写入 node.data.fieldPermissions（无暂存，所见即所存；`List<ProcessNodeFieldPermission>` = {fieldKey=字段 fieldId, permission}，types/process）随 rawData 保存，取消勾选/表单变更时连同 inheritMainForm 一起重置）；入 node.data 随 rawData 保存）；serviceTask → `ServiceTaskConfig`——delegateExpression 委托表达式下拉（`getDelegateExpressions` → `/processDefinition/delegateExpressions` 无参 → SelectOption[]，value=展示、key=入值）+ async 异步 Switch；值入 node.data.delegateExpression/.async 随 rawData 保存）/ 点连线显**连线配置** `EdgeConfig`（`onEdgeClick` 选中，与节点互斥；label 名称=React Flow 原生 `edge.label` 直接渲染在线上 + description 备注入 `edge.data.description`，清空 label 置 undefined 不入库；**网关出边条件**（仅源节点是排他/包容网关的出边显示，EdgeConfig 传 nodes 判源 kind）：isDefault 是否默认分支 Switch（默认 false；**默认分支不可配条件**——选默认时隐藏条件区并清 conditionType/nativeExpression，取消默认时 conditionType 按缺省 CUSTOM；**排他网关默认分支单选**——设某边为默认时 updateSelectedEdge 把同源其它出边 isDefault 一并清掉；**默认分支特殊样式**——displayEdges 渲染态注入紫色虚线（`#9333ea`/dark `#c084fc`，dasharray 7 4）+ 同色箭头 + 无 label 时补「默认」字样，样式不落库仅 isDefault 入 edge.data）、conditionType 条件类型 Select（NATIVE传统表达式/CUSTOM自定义，非默认分支缺省 CUSTOM，默认分支留空；**切类型清另一类型字段互斥**——切 NATIVE 清 ruleTree、切 CUSTOM 清 nativeExpression、选默认分支两者都清）、nativeExpression 传统表达式 Input（仅 NATIVE 显，等宽字体）、**ruleTree 自定义条件树（仅 CUSTOM 显，`GatewayConditionEditor`：右栏只放「配置条件(N 项)」按钮，点开宽 Dialog 编辑——递归 AND/OR 组可嵌套子组 + 条件行（字段+运算符+比较值），复用动态表单联动 `COND_LABEL`/按字段类型过滤运算符思路；条件字段经 `conditionSources.ts` 的 `CONDITION_SOURCES[category]` 加载（**可插拔分类注册表**，返回**带组字段** ConditionFieldGroup[]——当前仅 FORM_FIELD=`/dynamicForm/info` 主表单「未分组+各分组」，后续加申请人/部门等仅注册新条目；`flattenFieldGroups` 拍平供查字段定义），字段下拉 `ConditionFieldSelect` 用 `SelectGroup/SelectLabel` 按「未分组/各分组」分节展示（**选值仍是单个 fieldKey**），值控件**复用字段真实控件**（对齐动态表单联动：借 `createField`+`FIELD_REGISTRY[type].Control` 造临时字段渲染，选项列全含 visible=false；IN/NOT_IN 选项类用 `OptionMultiSelect` 标签多选/级联用多选级联真实控件、无选项退化逗号串文本，REGEX 正则框）；**需流程已绑定表单+版本**否则禁用提示；存 `edge.data.ruleTree`=`List<ProcessGatewayConditionNode>`{nodeType AND/OR/CONDITION, category 恒 FORM_FIELD, fieldKey=fieldId, operator, value(unknown：单值串/IN 数组/EMPTY 省略), sort 同级下标, children}，保存 `stripEdge` 归一化 sort/category 补全、默认分支或 NATIVE 时置空，与 nativeExpression 同级互斥；表单变更时连同节点继承一起重置）；两者随 rawData 保存，`stripEdge`/`edgesFromRaw` 已保留 label/data）/ 运行态显**模拟运行**）；**节点 `type` 直接用 BPMN 元素名**（`startEvent`/`serviceTask`/`endEvent`，后端转 BPMN 时 type 即元素 tag 零映射，前端出通用图 JSON、后端转 BPMN），属性存 `node.data`（运行高亮 `__active` 运行时注入、保存前 `stripNode`/`stripEdge` 剥离）；**id 符合 NCName**（BPMN id 是 xsd:ID：字母/下划线开头、仅字母数字）——节点 `n_<uuid去连字符>`、edge `e_<source>_<target>`（`createEdge` 统一生成，不用 `->` 因 `>` 非法）、初始 `start`/`end`。**保存**（新建草稿 `addProcessDefinition` → `POST /processDefinition/add`，body {processName/processCategory/processDescription/rawData:{nodes,edges}}，rawData 为剥离运行时字段的画布 JSON（nodes 剥 `__active`/`selected`/`dragging`/`measured`，**edges 只留语义 id/source/target/handle/label/data、剥 style/markerEnd——样式是渲染细节、由 createEdge 加载回填时统一补**）；校验名称非空否则引导到流程配置；保存成功 `onSaved` 回列表）。**保存=新建（`addProcessDefinition` → `POST /processDefinition/add`，id==null）/ 修改（`saveProcessDefinition` → `POST /processDefinition/save`，多 id）**；**编辑/查看加载**（`getProcessDefinitionInfo` → `POST /processDefinition/info` body{id}，回填元信息 + rawData 还原画布：nodes 直接还原、edges 由 `createEdge` 补回渲染样式；`loading` 初值在 state 声明算好，避免 effect 内同步 setState 见通用坑）。顶栏（返回+标题+「查看数据」（`ProcessDataDialog` 画布 {nodes,edges} JSON+复制）+**「模拟运行」**（`ProcessRunPanel`：从开始 BFS 到结束、逐步点亮节点+输出执行轨迹，运行态锁编辑/删除）+保存，发布仅列表操作列做））+ **只读查看**（`readOnly`：无保存、Input 禁用、画布 nodesDraggable/Connectable 关 + `deleteKeyCode=null` 不可编辑，但**可选中**（`elementsSelectable` 开）——点击节点看只读节点配置，仅顶栏「查看数据」）。`_components/`(ProcessListPanel, ProcessDesigner, ProcessDataDialog, ProcessRunPanel, UserTaskConfig, CandidateSelects, InheritMainFormField, NodeFieldPermissions, GatewayConditionEditor, ConditionFieldSelect, conditionSources, nodes)；`useProcessDefinitionPage`。
- `ai/system-prompt-manager/`：AI 系统提示词管理。扁平列表+分页+搜索（`useAiSystemPromptPage`）。列：描述/创建时间/更新时间/操作。列表不含 prompt：编辑开弹窗即 loading，`/ai/systemPrompt/info`（**query 传参 id**）回填 description+prompt 全量，失败给重试（防空表单覆盖已有提示词）；description+prompt 均必填；删除 AlertDialog → `/ai/systemPrompt/delete`（**query 传参 id**）。`(AiSystemPromptFormDialog)`；`lib/api/aiSystemPrompt`、`types/ai-system-prompt`。
- `displayBoard/`、`crawler-task-manager/`：占位。
- `system/system-prompt/`：系统提示（全局公告）管理——与 ai 系统提示词无关。扁平列表+分页+搜索（匹配 prompt，`useSystemPromptPage`）。列：提示消息/状态 Badge（**前端按 `deadTime` 与当前时间比较现算**生效中/已过期，不落库）/创建人/创建时间/截止时间/操作。**无编辑**（接口无 update，公告只发不改）：行内「查看」开详情弹窗（`/systemPrompt/info` body{id} 拉完整内容，失败重试）、删除 AlertDialog（`/systemPrompt/remove` body{id}）。新增弹窗 prompt+deadTime 均必填（截止时间=Popover+Calendar+time Input，提交补 `:00` 秒）。`(SystemPromptFormDialog, SystemPromptDetailDialog)`；`lib/api/systemPrompt`（函数加 `sys` 前缀避与 aiSystemPrompt 同名）、`types/system-prompt`。
