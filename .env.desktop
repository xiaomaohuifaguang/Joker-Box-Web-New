# 桌面（Tauri）构建专用环境变量——由 next.config.ts 在 TAURI_ENV_PLATFORM 存在时加载。
# 后端接口绝对地址（桌面壳 origin 是 tauri://localhost，不能用相对路径）。
# ★ 后端地址改这里 ★ 改完重新 npm run build:desktop（变量构建时内联进产物）。
NEXT_PUBLIC_API_BASE=https://joker.shien.wang/joker-box
